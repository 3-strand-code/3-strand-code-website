machine:
  python:
    version: 2.7.5

dependencies:
  cache_directories:
    - "node_modules"
    - "bower_components"
  pre:
    - pip install -r requirements.txt
  post:
    - npm install .

test:
  override:
    - python manage.py test
    - py.test tests/functional/
